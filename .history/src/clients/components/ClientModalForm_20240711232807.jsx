import React from 'react'
import { useClients } from '../../hooks/useClients'

export const ClientModalForm = () => {
  
    const {clientSelected, handlerAddClient} = useClients();
  
  
    return (
    <div>ClientModalForm</div>
  )
}
